import { Text } from "react-native";
import React from "react";
import ViewWrapper from "@/components/ViewWrapper";

const task = () => {
  return (
    <ViewWrapper>
      <Text style={{ color: "white", fontSize: 20 }}>task</Text>
    </ViewWrapper>
  );
};

export default task;
